export interface Credentials {
    email: string;
    password: string;
}

export interface RegistrationFields {
    email: string;
    password: string;
    fullname: string;
    birthdate: string;
}